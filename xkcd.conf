-- vim: ts=4 sw=4 noet ai cindent syntax=lua

conky.config = {
    alignment = 'top_right',
	update_interval=21600,
	double_buffer = true,
    own_window = true,
    own_window_type = 'desktop',
	minimum_width = 450,
	minimum_height = 450,
}

-- ${exec python3 dilbert.py}

conky.text = [[
${execp python3 /home/stevebeaty/src/conky/xkcd.py}
]]
